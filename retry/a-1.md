# 基礎計算
## 基数変換
- 2 => 10の変換方法
```
桁の重みを足していく
```
- 2 => 8, 16の変換方法
```
3/4桁ごとに、0 ~ 7/Fに治す
```
- 10 => 2の変換方法
```
2で割っていって、余りをメモしてく
```
- 16 => 2の変換方法
```
1桁ずつ、2進数に変換していく、必ず4桁になるように0で埋める
```
- 10 => 16の変換方法
```
16で割ってく以下略
```
- 2に変換したら無限小数になる10進数を書いて
```
0.2
```

## bit計算
- 2の補数とは？
```
2進数を反転させて+1にしたもの
```
- 絶対値とは？
```
符号を覗いた数の事
```
- 符号あり00110110のマイナス表現は？
```
11001010
```
- 10010011を符号ありと無しで、それぞれ2倍, 1/2倍して
```
なし
オーバーフロー
01001001 余り 1
あり
オーバーフロー
11001001 余り 1
```
- bit同士の掛け算と割り算のやり方

https://www.fe-siken.com/kakomon/18_haru/q15.html
- マスクパターンとは？
```
bitを取り出すために用意された1の列
```

## 浮動小数
- 符号、仮数、基数、指数とは？
```
符号 マイナス表記
仮数 0を取った残り
基数 n
指数 nの-何乗か
```
- 指数表記の説明
```
0.Mの形にする
符号, 10のマイナス何乗か, Mで表す
1bit, 7bit, 24bit
```
- IEEE754形式の説明
```
1.Mの形にする
符号, 10のマイナス何乗か, Mで表す
1bit, 8bit, 23bit
指数に127をプラスした値で計算する
```

## 誤差
- 情報落ち、打切り誤差、桁落ち、丸め誤差の説明
```
情報落ち 大きい値と小さい値で足したときに、小さい方が足されなくなる
打切り誤差 計算が途中で終了
桁落ち 余り変わらない数で足し引きした時に、正規化した際の0のところが信用できなくなる
丸目誤差 四捨五入
```
- 誤差を抑える工夫を1つ
```
小さい数から足す
```

## 逆ポーランド記法
- https://www.fe-siken.com/kakomon/21_aki/q3.html

## 論理演算 / 回路
- AND, OR, NOTの説明
```
どっちも
どちらか
反転
```
- NAND, NOR, ERO(XOR)の説明
```
片方以上が1ではない
どちらも0
両者が異なる値
```
- 各論理計算子の図記号を書いて
- ド・モルガンの法則とは？
```
_(a ・ b) = (_a) + (_b)
```
- https://www.fe-siken.com/kakomon/25_aki/q1.html
- https://www.fe-siken.com/kakomon/28_haru/q23.html

## 単位
- { k, M, m, n, μ, T, p, G, 無し } 左のbyteの単位を大きい順位並べよ
```
T, G, M, k, 無し, m, μ, n, p
```
- 上の単位は10の何乗ずつ変化する？
```
3乗
```

# データの表現
## 文字コード
- 文字コードの仕組みは？
```
決められたbyteの組合わせによって表示指せる物を変えている
```
- ASCII, S-JIS, Unicode
```
ASCII 半角英数と一部の記号、オーソドックス
S-JIS 日本語対応可能で、マイクロソフトに使われている
Unicode 全か国語対応
```

## デジタル
- デジタルとは？
```
区切りのあるデータ
```
- 画像の色に必要なbit数、16色、256色(グレースケール)、16,777,216色(フルカラー)それぞれいくつか
```
4bit
8bit
24bit
```
- 音声データの符号化、標本化、量子化とは？
```
標本化 区切る
量子化 音階に分ける
符号化 bitにあてはめる
```
- 音声データのサンプリング周波数とは？
```
1秒間に何かい標本化するか
```

## コンピュータ制御
- アクチュエータとは？
```
電子信号によって機械を動かして、物理エネルギーを生み出す
```
- シーケンス制御とは？
```
順番に決められた事を行う
```
- {ひずみケージ、ジャイロセンサ} 左のセンサの説明
```
橋等の歪みを感知
傾き、回転を感知
```
# ハードウェア
## 基礎
- ノイマン型コンピュータとは
```
補助記憶装置から主記憶装置へ
主記憶装置からCPUへ
一つ一つデータを取り出して処理していく
```
- アドレスとは
```
データの保管場所
```

## CPU
- 以下の説明
  - プログラムカウンタ, 命令レジスタ, インデックスレジスタ, ベースレジスタ, アキュムレータ, 汎用レジスタ
```
プログラム 次の命令のアドレス
命令レジスタ 実行する命令
インデックス アドレス指定用、並列処理に使われる
ベース 最初の命令を保存しておく
アキュムレータ 計算処理結果等
汎用レジスタ 汎用
```
- CPUの命令の実行の流れの説明
  - フィッチ, オペランド部の読み出し, 解読
```
フィッチ 命令を取り出す、プログラムカウンタの更新
解読 命令デコーダに命令を渡す
オペランド部 データを取ってくる
```
- 以下のアドレス指定方式の説明
  - インデックス, ベースアドレス, 相対アドレス
```
  インデックスレジスタに記憶されている物
  ベースレジスタのアドレスを足す
  プログラムカウンタのアドレスを足す
```
- 以下の説明
  - アドレスバス, データバス, コントロールバス, I/Oバス
```
アドレスパス アドレスを伝える
データパス メモリとCPUがデータを渡す
コントロールパス CPUが外部と
I/Oパス 入出力
```
- 性能についての説明
  - クロック周波数, CPI, MIPS, 命令ミックス
```
クロック周波数 1秒間に何クロック回るか
CPI Clock System Per Instruction 何クロックで命令をこなせるか
MIPS Million Instruction Per Second 1秒でなん百万の命令をこなせるか
命令ミックス よくある命令の詰め合わせ、性能を計るときに使う
```
- パイプライン処理とは？
```
命令の1ステップが終わるごとに、違う命令の1ステップを行い、無駄なくCPU等を使う
```
- 分岐ハザードと投機実行とは？
```
分岐ハザード 予測で行った処理が無駄になる事
投機実行 予測先の処理を分岐を待たずに行う事
```
- CISCとRISCの違いは？
```
CISC 複雑な事を1命令で行えるようにする
RISC 単純な命令を高速で行えるようにする
```

## メモリ
- DRAM, SRAMの{用途, 使用してる回路等}を答えて
```
DRAM 主記憶装置 コンデンサ
SRAM キャッシュメモリ フリップフロップ回路
```
- 以下の説明
  - ROM, PROM, EEPROM, フラッシュメモリ
```
ROM Read Only Memory
PROM ユーザの手で書き換えられるメモリ
EEPROM 電気で保存
フラッシュメモリ EEPROMの一種で、ブロック単位の保存が可能
```
- キャッシュとは？
```
データを一時保存しておく、主記憶との中間、高速化
```
- ディスクキャッシュとは？
```
主記憶と補助記憶のキャッシュ
```
- ライトスルー方式とライトバック方式とは？
```
ライトスルーがミラーリング
ライトバックが溢れたら主記憶を書き換える方式
```
- メモリインタリーブとは？
```
データを何個かの塊に分けて保存
連続したデータを一気に取り出せる
```

## 補助記憶装置
- ハードディスクについて以下の説明
  - セクタ, トラック, シリンダ
```
セクタ 1単位
トラック セクタ1周文
シリンダ トラックの縦
```
- ハードディスクの読み込み工程3段階の名前は？
```
シーク, サーチ, 読み込み
```
- ハードディスクの平均アクセス時間を求める式は？
```
シークの平均 + サーチの平均 + データの転送時間の平均
左は固定
```
- フラグメンテーションとは？
```
セクタに余りが出てしまう事
```
- RSID 0, 1, 5の説明
```
0 ストライピング 高速化
1 ミラーリング 信頼
5 3つのディスクで、一つにパリティを仕込む
```
- SSDとは？
```
フラッシュメモリの集合体、待ち時間がなく高速
```
- レーザーディスク(CD, DBD)の仕組み
```
表面に傷を付けて、それをレーザーやブルーライトで読み取る
```
- 磁気テープの使用する媒体と用途
```
磁気、バックアップ
```

## 入出力装置
- ポインティングデバイスの例3つ
```
トラックパッド
マウス
タッチパネル
```
- OCR, OMR, イメージスキャナの説明
```
OCR 文字を読み取る
OMR マークシートを読み取る
イメージスキャナ 画像を読み取る
```
- QRは何で角度や方向を認識できる？
```
3つの検出用シンボル
```
- 以下のディスプレイは、それぞれ何を使用して発光、制御しているか
  - 液晶, 有機LE, プラズマ
```
電圧
有機化合物
プラズマ放電
```
- 以下のプリンタの印刷方法は？
  - ドットインパクト, インクジェット, レーザ
```
先のインク
インクを吹きかける
レーザーの上にトナー
```
- 性能指標 dpi, cps, ppmはそれぞれなんの略称？
```
dpi 1インチに何ドットか
cps 文字/s
ppm ページ/m
```

## 入出力装置インターフェース
- 入出力装置インターフェースとは？
```
機器を繋げるときの、ゲーブルの差込口等の企画
```
- パラレルとシリアルとは？
```
複数のデータを並列で送るものと、単品
```
- USBの企画のUSB3.0は、何bit/m？
```
5Gbit/m
```
- Bluetoothの特徴一つ
```
壁の影響を受けない
```

# ソフトウェア
## 基礎
- APIとは何の略？
```
Application Program Interface
```
- スループットとは？
```
1ジョブ辺りの時間
```
- プロセッサとは？
```
プロセスを実行する装置
```

## ジョブ管理
- ジョブとは？
```
1つの仕事の単位
```
- パッチ処理とは？
```
待ち時間を減らすために、処理をまとめて命令して機械にやらせる事
```
- マスタスケジューラとシステムスケジューラとは？
```
マスタスケジューラが人と機械のインターフェース
システムスケジューラがジョブ管理を行う
```
- システムスケジューラの処理の流れ
  - リーダ, イニシエータ, ターミネータ, ライタ
```
リーダ 命令をキューに入れる
イニシエータ 優先度の高い命令を取得、資源を割り当てる
ターミネータ 資源の開放とライタへの受け渡し
ライタ 出力する
```
- スプーリングとは？
```
CPUとハードウェアの処理速度を改善するための、メモリのような役割をする磁気ディスク
```

## タスク管理
- タスクの状態3つ
```
wait
ready
run
```
- ディスパッチャとは？
```
資源を割り当てる
```
- 割り込みに関して
https://www.fe-siken.com/kakomon/22_aki/q11.html
https://www.fe-siken.com/kakomon/13_aki/q21.html
https://www.fe-siken.com/kakomon/20_aki/q18.html
https://www.fe-siken.com/kakomon/18_haru/q21.html
https://www.fe-siken.com/kakomon/23_toku/q9.html
https://www.fe-siken.com/kakomon/25_haru/q11.html

## 実記憶管理
- オーバーレイとスワッピング、2つの方式について
```
区間毎に読み込んで、読み込める量を増やす
優先度の低いジョブを補助記憶に飛ばす
```

## プログラムの性質
- 以下の説明
  - リロケータブル、リユーザブル、リエントラント、リカーシブ
```
リロケータブル どこにでも読み込める
リユーザブル 読み込み直さなくても使える
リエントラント 並列で呼び出されても正しい結果を返す
リカーシブ 自分を呼び出せる
```

## 仮想記憶
- 仮想記憶の仕組み
```
補助記憶装置をメモリとして使う事で、実際よりも多くの容量のプログラムを実行する
```
- ページング方式の説明
```
ページで実記憶のアドレスと実際のアドレスを管理する
```
- スラッシングとは
```
ページングのやりすぎで処理が遅くなること
```
- FIFO, LRU, LIFO, LFUの説明(recently, frequently)
```
FIFO First in First out: キュー
Last in First out: スタック
LRU recently もっとも最近利用されていないものから
LFU frequently もっとも利用回数が少ないものから
```

## DB
- スキーマの説明
  - 内部、概念、外部
```
物理的な保存場所
本体
プログラムが弄れる範囲
```
- ACIDとは？
```
Atomicity 完了したか実行前か
Consistency 矛盾していない
Isolation 排他制御
Durability 壊れた時の保険がある
```
- ジャーナルとは？
```
ログファイル
```
- KVSに置けるBASEとは？
```
Basecally Available 基本的に利用可能
Soft-state 柔軟な状態、最終的な完全性
Eventual consistency 最終的に矛盾がない
```
- ビックデータとは？
```
常に変化する、大量のデータ
```
- データウェアハウスとは？
```
企業の方針を助けるデータ
```
- 以下の説明
  - データマート, データマイニング, データクレンジング
```
マート 特定のデータをまとめたもの
マイニング 法則性を見出す
クレンジング データのズレや矛盾を取り除くこと
```
- データの結合について、ソートマージとハッシュの説明
```
合う順番に並び替える
ハッシュ関数を作って結合
```

# ファイル
## データ
- 画像圧縮の仕組み
```
連続したbitやbyteを、*いくつで合わせて容量を小さくする
```
- 不可逆、可逆どっちか
  - bmp, jpeg, gif, png, mp3, mpeg
```
gitとpngのみ可逆
```

## アクセス
- 直接編成ファイルとは？
```
ハッシュや配列
```
- 区分編成ファイルとは？
```
メンバと呼ばれる順編成ファイル
```

# その他
## 電力
- ワット(W), ボルト(V), アンペア(A)の式
```
W = V * A
```

## 3DCG
- 以下の説明
  - クリッピング, レイトレーシング, アンチエイリアシング, ジャギー, シェーディング, シャドーイング, テクスチャマッピング
```
クリッピング 範囲外を削除
レイトレーシング 物体と光との交点ごとに、一番手前のポリゴンを探す
ジャギー/アンチエイリアシング ポリゴンの角(ジャギー)を取る
シェーディング 色の濃淡を書き分ける
シャドーイング 陰を付ける
テクスチャマッピング 細かい凹凸で素材感を出す
```

# ネットワーク
## WAN / LAN
- パケット交換方式とは？
```
データをパケットというのに分解して、行先情報を付ける
```
- イーサネットとは？
```
LAN
```
- バス型・スター型、トークンリングそれぞれのアクセス制御法の説明
```
CSMA/CD イーサネットが使われていない事を確認して、データを送る
トークンバッシング データを回していく
```

## プロトコル
- プロトコルとは？
```
ネットワークでのルールや規格
```
- OSI基本参照モデル、全7層
```
1 物理層
2 データリング層
3 ネットワーク層
4 トランスポート層
5 セッション層
6 プレゼンテーション層
7 アプリケーション層
```
- TCP/IPとは？
```
TCP IPの制御、セキュリティやエラーの調整
IP パケットをリレーで渡していく
```
- 以下のプロトコルの説明
  - PPP, ARP, SMTP, POP3, MIME, HTTP, DNS, DHCP, NTP
```
PPP 電子メールを送信
ARP Address Relolution Protocol IPアドレスからMACアドレスを求める
SMTP Simple Mail Transfer Protocol
POP3 メール
MIME メールの内容に日本語等を適応させる
HTTP webに利用される、ハイパーテキストを蜘蛛の巣状に繋げる
DNS Domain Name System
DHCP Dynamic Host Confugration Protocol IPの割当
NTP Network Time Protocol
```
- 以下のポート番号に対応したプロトコル
  - 80, 110
```
HTTP, POP3
```

## ネットワーク機器
- 以下の説明
  - NIC, リピーター, ブリッジ, ルータ, ゲートウェイ
```
NIC macアドレスを所持している、データと電気信号の変換を行っているカード
リピーター 電気信号をそう服する
ブリッジ セグメント同士を繋げる
ルータ LAN同士を繋げる
ゲートウェイ 異なるプロトコル同士を翻訳する
```
- レイア2スイッチ, レイア3スイッチとは？
```
2層、3層のスイッチ、つまりスイッチングハブとルータ
```
- MACアドレスとは？
```
前半メーカー番号、後半製造番号
```

## 制御
- パリティチェック, CRCの説明
```
1bit追加して、基数か偶数か揃えて、違っていたらエラーを検出出来る
bit列を生成多項式で割って、割り切れない場合誤り
```

## TCP/IP
- ブロードキャストとは？
```
全ホストに一斉にデータを送る事
ホスト番号をすべて1で埋める
```
- NAT, NAPT(IPマスカレード)の説明
```
NAT 1:1でグローバルとプライベートIPアドレスを結び付ける
NAPT 複数のIPアドレスがない機器にアドレスを割り振る
```
- IPv6の追加機能2つ以上
```
IPsec搭載
アドレス自動登録
効率的なルーティング
```

## 電子メール
- メールアドレスは〇〇@✖✖、当てはまるものは？
```
ユーザ名@ドメイン名
```
- TO, CC, BCCとは？
```
宛先, サブの宛先, 非公開のCC
```

## その他
- Cookieとは？
```
データの一部を一時的にクライアントに保存しておくシステム
```
- キャリアアグリケーションとは？
```
複数の回線を1本として使う
```

# セキュリティ
## セキュリティマネジメント
- セキュリティマネジメント3原則の説明
  - 機密性, 完全性, 可用性
```
セキュリティ、データに欠けがない、利用するべき時に利用できる
```
- その他の4要素は？
```
責任追随性
真正性
信頼性
否認防止
```
- BYODとは？
```
個人の機器を業務に使って、セキュリティリスクを負うこと
```
- スニファとは？
```
パケットを監視するツール
```
- キーロガーとは？
```
キーの入力情報を保存するツール
```
- リスクマネジメント3要素
```
```
- リスクコントロールについて以下の具体例
  - 軽減, 回避, 分散, 集中
```
安全な機器を使う
ハイリスクな業務をしない
アウトソージング
管理コストがかかっているところに集中させる
```
- リスクファイナンシングについて以下の説明
  - 移転, 保有
```
保険
甘んじて無視する
```


## 攻撃
- 以下の説明
  - DoS攻撃, DDoS攻撃, バッファオーバーフロー攻撃
```
  大量のパケットを送る
  ↑を複数人でやる
  データを大量に送り付けて、損害を与える
```

## 認証
- 以下の説明
  - バイオメトリクス認証, タイムスタンプ認証, メッセージ認証, チャレンジレスポンス認証
```
生体, その時間のアリバイ保存, ディジタル署名, サーバーからのリクエストに答える
```
- アクセス権4種
```
読み取り
書き込み
編集
削除
```
- ソーシャルエンジニアリングとは？
```
システムと関係ないところで情報を盗む
```

## 不正ログイン
- 不正のトライアングル3つ
```
動機, 機会, 正当化
```
- ブルートフォース攻撃とは？
```
パスワードを順に入力していく
```
- DNSキャッシュポイズニングとは？
```
クライアントのキャッシュに悪意のあるプログラムを仕込む
```
- rootkitとは？
```
バックドアを設置する等、攻撃の準備をするキット
```

## コンピュータウイルス
- 以下の説明
  - ウイルスの正式な意味、ワーム、トロイの木馬、ボット
```
他のプログラムに寄生して、不正な動作をする
増殖する
表向き良いソフトとしてふるまう
遠隔操作される
```
- パターンマッチング方式、ビヘイビア法の説明
```
リストの中の情報と似た信号をキャッチしたら反応
常に監視して、怪しかったら仮想環境で実行
```

## ファイアフォール
- ファイアフォールとは？
```
決まった形のない、セキュリティ的な壁の事
```
- パケットフィルタリングとは？
```
特定のパケットをブロックする
```
- アプリケーションゲートウェイ(プロキシサーバ)とは？
```
サーバーを外に晒さずに済むよう、外部と代わりにやり取りする仲介
```
- DMZとは？
```
外部からのアクセスを許可しているものを置く場所
```
- 構成について
https://www.fe-siken.com/kakomon/26_aki/q40.html
- ペネトレーションテストの説明
```
実際の攻撃テスト、予測不能なデータを大量に送る
```
- 以下の説明
  - SIEM, IDS, MDM
```
SIEM リアルタイムでログを監視
IDS 挙動を確認して不正な侵入を防ぐ
MDM モバイル端末用の管理ツール
```

## 暗号化
- 以下の説明
  - 共通鍵暗号方式、公開鍵暗号方式、ディジタル署名
```
お互いに共通の鍵で暗号化、解読を行う
公開鍵で暗号化、秘密鍵で解読
本文をハッシュ化したものを本文にくっつける
```
- CA(Certificate Authority)とは？
```
公開鍵を登録して証明出来る機関
```
- 以下の説明
  - RSA, 楕円曲線暗号
```
RSA 素因数分解
楕円曲線 RSAと同じ長さでより高速
```

## ネットワークセキュリティ
- 以下のプロトコルの説明
  - SSL/TLS, HTTPS, IPsec, S/MIME, WPA
```
SSL/TLS 
HTTPS HTTPに承認ヘッダを加えたもの
IPsec 承認ヘッダを加えたIP
S/MINE MINEに承認機能を付けたもの
WPS Wi-FIのセキュリティ規格
```
- 以下のセキュリティ技術の説明
  - VPN, ポートスキャナ, ハニーポット
```
VPN
ポートスキャナ 現在動いているサービスを確認する
ハニーポット わざと脆弱性のあるプログラムを置いておく
```

# システム開発
## ソフトウェアライフサイクル
### 企画プロセス
- 企画プロセスで検討する項目5つ
```
スケジュール
体制
リスク
費用対効果(投資)
運用範囲(スコープ)
```
- 開発依頼の流れの穴埋め
1.xx側がxx側へ〇〇の提出
2.xx側が〇〇の作成
3.xx側が〇〇を受け取る
4.xx側が〇〇を受け取る
5.契約
```
依頼主 システムベンダ RFI(Request For Information)
システムベンダ RFP(Request For Proposal)
依頼主 提案書
依頼主 見積書
```

### 要件定義プロセス
- 要件定義では何を行う？
```
ヒアリング等で、機能を明確にする
要件定義書の作成
```
- システム設計の内、外部設計、内部設計ではそれぞれ何を行う？
```
外部 インターフェース、レイアウト
内部 DB設計
```

## 開発モデル
- 以下の開発モデルの説明
  - ウォーターフォール, プロトタイピング, スパイラル, アジャイル, フォワードエンジニアリング
```
順番, 1度最初にプロトタイプを作る, プロトタイプを永久に作る, システムを細かくリリースする, 既にあるものから作る
```
- eXtreme Programmingの5つの価値は？
```
コミュニケーション
シンプル
フィードバック
勇気
尊重
```
- eXtreme Programmingのペアプログラミング, ソースコードの扱いの説明
```
二人一組で、モデレーターとコーダーを交代しながら進める
ソースコードは誰が編集しても良い
```

## コード設計
- 入力チェックの内、以下の説明
  - シーケンス, 照合, 重複, 論理
```
順序, 入力データの重複, 既存データとの重複, 矛盾がないか
```

## モジュール
- モジュールの強度と結合度の説明
```
どの程度の繋がりの機能を受け持っているか
他のモジュールとの関わり方
```
- 強弱
https://www.fe-siken.com/kakomon/29_aki/q48.html
https://www.fe-siken.com/kakomon/28_haru/q47.html
https://www.ap-siken.com/kakomon/21_aki/q45.html
https://www.fe-siken.com/kakomon/19_haru/q43.html
https://www.fe-siken.com/kakomon/23_aki/q45.html
https://www.ap-siken.com/kakomon/23_toku/q47.html

## テスト
- 結合テスト, システムテストとは？
```
モジュール同士を繋げた時のテスト
システム全体のテスト
```
- ブラックボックステストとホワイトボックステストの説明
```
ブラックボックス テストデータを用意して、正しい結果が返るかを見る
ホワイトボックス 条件分岐等、中の動作を見る
```
- 条件網羅について、以下の説明
  - 特定条件網羅, 条件網羅, 複数条件網羅
```
分岐の内、true, falseをそれぞれ一回ずつ通ればよい
それぞれの条件の内、true, falseになるデータをそれぞれ一回通せばよい
全ての条件の全てのtrue, falseの組合わせを試す
```
- スタブとドライバとは？
```
単体、結合テストでの仮モジュール
スタブが下位、ドライバが上位
```
- リグレッションテストとは？
```
修正によって、他のモジュールに影響が出ていないか調べる
```
- 信頼度成長曲線とはどんな形？
```
バグが途中から大きく増え、徐々に減っていく
```

## レビュー
- ウォークスルーとインスベクションの説明
```
開発者と提案者全員で
モデレーター主軸で
```

## 開発環境管理
- 以下の具体例
  - 開発環境稼働状況管理, 設計データ管理, ツール管理, 編成管理, 変更管理, リリース管理
```
  資源の確認, バージョン管理, ツールの種類等を統一してセキュリティホールを減らす, 使用するハードの変更、計画変更の策定, バージョン、保管期間
```

## 再利用
- 再利用可能なソフトウェアの特徴
```
汎用性に長ける、より丁寧なテストが必要
```
- ソフトウェアパッケージとは？
```
企業がソフトを開発する時の基盤になるパッケージ
```
