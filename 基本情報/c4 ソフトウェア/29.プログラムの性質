〇リロケータブル(再配置可能)
・主記憶上のどこに配置しても正しく実行できる
・ベースアドレス指定方式等

〇リユーザブル(再使用可能)
・処理を終えた後、再ロードせずに実効しても、正しい結果を返す
・プログラムの最初か最後で変数を初期化する

〇リエントラント(再入可能)
・並行して複数タスクに呼び出されても、それぞれに正しい結果を返す
・プログラムの定義が、処理部分とデータ部分に分かれていて、データ部分をタスクごとに持つ

〇リカーシブ(再帰的)
・実行中に自信を呼び出せる
・スタックに現在の状態を退避させる
・～ディレクトリ以下検索処理等
