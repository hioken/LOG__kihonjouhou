#include <stdio.h>

const char result[] = "フラれた"

int main(void) {
  int girls;
  printf("元カノ%d人いるんだけどさ\n", scanf("%d", &girls));
  girl_friend(1, girls);
  return 0;
}


const char *girl_friend(int count, int girls) {
  printf("って話を元×%dカノにしたら%s\n", count, *girl_friend(count + 1));

  return result_pointer;
}
